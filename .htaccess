<IfModule mod_rewrite.c>
    RewriteEngine On

    # 1. Enforce Non-WWW 301 Permanent Redirect (www.projectstore.pk -> https://projectstore.pk)
    RewriteCond %{HTTP_HOST} ^www\.projectstore\.pk$ [NC]
    RewriteRule ^(.*)$ https://projectstore.pk/$1 [R=301,L]

    # 2. Enforce HTTPS on Apex Domain
    RewriteCond %{HTTPS} off
    RewriteCond %{HTTP:X-Forwarded-Proto} !https
    RewriteCond %{HTTP_HOST} ^projectstore\.pk$ [NC]
    RewriteRule ^(.*)$ https://projectstore.pk/$1 [R=301,L]
</IfModule>

# Browser Caching for Static and Media assets in cPanel
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType font/woff2 "access plus 1 month"
</IfModule>

# Security Headers
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>
